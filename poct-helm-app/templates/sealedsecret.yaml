apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: my-tls-secret      
  namespace: argotest       
spec:
  encryptedData:
    tls.crt: <ENCRYPTED_CRT>  # Sealed value (generated using kubeseal)
    tls.key: <ENCRYPTED_KEY>  # Sealed value (generated using kubeseal)
