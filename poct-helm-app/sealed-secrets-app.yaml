apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: poct-helm-sealedsecret
  namespace: argotest
spec:
  project: default
  source:
    repoURL: 'https://github.com/mmostofian/ArgoCD/blob/main/poct-helm-app.git'
    targetRevision: HEAD
    path: .           
    helm:
      valueFiles:
        - values.yaml
  destination:
    server: 'https://kubernetes.default.svc'
    namespace: argotest  
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
