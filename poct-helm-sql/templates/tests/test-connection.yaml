apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "poct.fullname" . }}-test-connection"
  labels:
    {{- include "poct.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "poct.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
